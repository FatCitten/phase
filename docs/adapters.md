# Agent adapters

Phase adapters translate a fiber invocation into a non-interactive agent CLI call. Built-in detection supports Pi, Codex CLI, Claude Code, Gemini CLI, OpenCode, and Aider. Generic executables and JSON manifests are supported for other harnesses.

The Phase runtime remains the outer resource/provenance layer. Agent-specific permissions are not treated as equivalent to OS isolation.
